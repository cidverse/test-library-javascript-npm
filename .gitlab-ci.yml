
# cid-workflow-version: 0.9.0

# This file is generated by the CID Workflow GitLab App.
# DO NOT EDIT!

# global defaults
default:
  image: quay.io/podman/stable@sha256:42d8b3d789333f07e631305325a02b4ab33c3f107413e04b612973a72f1d7deb # v5.8.1-immutable
  before_script:
    - bash .cid/scripts/install.sh "0.9.0" ""

# variables
variables:
  # git settings
  GIT_STRATEGY: clone
  GIT_DEPTH: 10

# stages
stages:
    - build
    - test
    - lint
    - scan
    - publish

# configuration snippets
.default-job: &default_job
  
  tags:
    - saas-linux-small-amd64
  allow_failure: false
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - api_failure
      - scheduler_failure

# Nightly
## npm-build [test-library-javascript-npm]
"nightly/npm-build [test-library-javascript-npm]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "npm-build-test-library-javascript-npm"
    artifacts:
      name: "nightly/npm-build [test-library-javascript-npm]"
      paths:
        - .dist/npm-build-test-library-javascript-npm/
      expire_in: 1 day
## npm-test [test-library-javascript-npm]
"nightly/npm-test [test-library-javascript-npm]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "npm-test-test-library-javascript-npm"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "nightly/npm-test [test-library-javascript-npm]"
      paths:
        - .dist/npm-test-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        junit: .dist/npm-test-test-library-javascript-npm/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: cobertura
          path: .dist/npm-test-test-library-javascript-npm/report/cobertura/*.xml
## npm-lint [test-library-javascript-npm]
"nightly/npm-lint [test-library-javascript-npm]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "npm-lint-test-library-javascript-npm"
    artifacts:
      name: "nightly/npm-lint [test-library-javascript-npm]"
      paths:
        - .dist/npm-lint-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        codequality: .dist/npm-lint-test-library-javascript-npm/report/gl-codequality/*.json
## gitleaks-scan
"nightly/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gitleaks-scan"
    artifacts:
      name: "nightly/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"nightly/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "semgrep-scan"
    artifacts:
      name: "nightly/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"nightly/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "trivyfs-scan"
    artifacts:
      name: "nightly/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Pull Request
## npm-build [test-library-javascript-npm]
"pull-request/npm-build [test-library-javascript-npm]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "npm-build-test-library-javascript-npm"
    artifacts:
      name: "pull-request/npm-build [test-library-javascript-npm]"
      paths:
        - .dist/npm-build-test-library-javascript-npm/
      expire_in: 1 day
## npm-test [test-library-javascript-npm]
"pull-request/npm-test [test-library-javascript-npm]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "npm-test-test-library-javascript-npm"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "pull-request/npm-test [test-library-javascript-npm]"
      paths:
        - .dist/npm-test-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        junit: .dist/npm-test-test-library-javascript-npm/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: cobertura
          path: .dist/npm-test-test-library-javascript-npm/report/cobertura/*.xml
## npm-lint [test-library-javascript-npm]
"pull-request/npm-lint [test-library-javascript-npm]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "npm-lint-test-library-javascript-npm"
    artifacts:
      name: "pull-request/npm-lint [test-library-javascript-npm]"
      paths:
        - .dist/npm-lint-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        codequality: .dist/npm-lint-test-library-javascript-npm/report/gl-codequality/*.json
## gitleaks-scan
"pull-request/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gitleaks-scan"
    artifacts:
      name: "pull-request/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"pull-request/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "semgrep-scan"
    artifacts:
      name: "pull-request/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"pull-request/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "trivyfs-scan"
    artifacts:
      name: "pull-request/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Release
## npm-build [test-library-javascript-npm]
"release/npm-build [test-library-javascript-npm]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "npm-build-test-library-javascript-npm"
    artifacts:
      name: "release/npm-build [test-library-javascript-npm]"
      paths:
        - .dist/npm-build-test-library-javascript-npm/
      expire_in: 1 day
## npm-test [test-library-javascript-npm]
"release/npm-test [test-library-javascript-npm]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "npm-test-test-library-javascript-npm"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "release/npm-test [test-library-javascript-npm]"
      paths:
        - .dist/npm-test-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        junit: .dist/npm-test-test-library-javascript-npm/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: cobertura
          path: .dist/npm-test-test-library-javascript-npm/report/cobertura/*.xml
## gitleaks-scan
"release/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitleaks-scan"
    artifacts:
      name: "release/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"release/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "semgrep-scan"
    artifacts:
      name: "release/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"release/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "trivyfs-scan"
    artifacts:
      name: "release/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
## changelog-generate
"release/changelog-generate":
    <<: *default_job
    stage: publish
    needs: ["release/gitleaks-scan", "release/npm-build [test-library-javascript-npm]", "release/npm-test [test-library-javascript-npm]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "changelog-generate"
    artifacts:
      name: "release/changelog-generate"
      paths:
        - .dist/changelog-generate/
      expire_in: 1 day
## gitlab-release-publish
"release/gitlab-release-publish":
    <<: *default_job
    stage: publish
    needs: ["release/changelog-generate", "release/gitleaks-scan", "release/npm-build [test-library-javascript-npm]", "release/npm-test [test-library-javascript-npm]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: ["release/changelog-generate"]
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitlab-release-publish"
    artifacts:
      name: "release/gitlab-release-publish"
      paths:
        - .dist/gitlab-release-publish/
      expire_in: 1 day

# Main
## npm-build [test-library-javascript-npm]
"main/npm-build [test-library-javascript-npm]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "npm-build-test-library-javascript-npm"
    artifacts:
      name: "main/npm-build [test-library-javascript-npm]"
      paths:
        - .dist/npm-build-test-library-javascript-npm/
      expire_in: 1 day
## npm-test [test-library-javascript-npm]
"main/npm-test [test-library-javascript-npm]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "npm-test-test-library-javascript-npm"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "main/npm-test [test-library-javascript-npm]"
      paths:
        - .dist/npm-test-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        junit: .dist/npm-test-test-library-javascript-npm/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: cobertura
          path: .dist/npm-test-test-library-javascript-npm/report/cobertura/*.xml
## npm-lint [test-library-javascript-npm]
"main/npm-lint [test-library-javascript-npm]":
    <<: *default_job
    stage: lint
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "npm-lint-test-library-javascript-npm"
    artifacts:
      name: "main/npm-lint [test-library-javascript-npm]"
      paths:
        - .dist/npm-lint-test-library-javascript-npm/
      expire_in: 1 day
      reports:
        codequality: .dist/npm-lint-test-library-javascript-npm/report/gl-codequality/*.json
## gitleaks-scan
"main/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gitleaks-scan"
    artifacts:
      name: "main/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"main/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "semgrep-scan"
    artifacts:
      name: "main/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"main/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "trivyfs-scan"
    artifacts:
      name: "main/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
